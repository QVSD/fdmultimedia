package com.fdmultimedia.api.contentsuggestions;

/**
 * SOCIAL_COPY is the only type in Phase 12A: one generation request produces
 * one coherent package (hook + caption + hashtags + optional short title) —
 * never four independent AI jobs for the same Draft.
 */
public enum ContentSuggestionType {
    SOCIAL_COPY
}
