/**
 * Publishing generated media to external platforms.
 *
 * Phase 10A: the controlled foundation only — a durable {@code Publication}
 * (intent + state) and {@code PublishingAttempt} (per-attempt history),
 * executed through the existing distributed {@code PUBLISH_MEDIA} Job
 * infrastructure and a deterministic {@code TEST} provider. No real
 * Instagram/TikTok integration exists yet.
 */
package com.fdmultimedia.api.publishing;
